// import test, { expect } from "@playwright/test";
//
//
// test('first test (trial only)', async ({ page }) => {
//   let response = await page.goto("https://www.google.com/search?q=nifty+50+futures", {})
//   if (response === null) {
//     test.fail("the response form going to url is null", ({ page }) => {
//       console.log(`the page we got in the fail function is ${JSON.stringify(page.request)} \n`);
//
//     })
//   }
//   console.log(`the response is not null ${JSON.stringify(response)}`);
//
//   expect(page).toHaveTitle('/nifty 50/')
//
// })
